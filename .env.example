# Postgres (used by docker-compose and, later, by the app's DB connection config)
POSTGRES_USER=izavet
POSTGRES_PASSWORD=change-me
POSTGRES_DB=izavet
POSTGRES_PORT=5432

# API (used by docker-compose when running the api container)
API_PORT=3000
